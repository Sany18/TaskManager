header.w3-container.w3-light-grey
  h1.w3-xxxlarge
    | Task management system
.w3-container.w3-teal
  h2
    | Sing in
.w3-container.w3-teal
  p.notice
    = notice
  p.alert
    = alert
.w3-container.w3-margin-top[style="width:500px"]
  = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    .field
      = f.label :email
      br
      = f.email_field :email, autofocus: true, autocomplete: "email", class: "w3-input w3-border w3-round-large"
    .field
      = f.label "Password"
      br
      = f.password_field :password, autocomplete: "current-password", class: "w3-input w3-border w3-round-large"
    - if devise_mapping.rememberable?
      .field.w3-margin-top.w3-margin-bottom
        = f.check_box :remember_me
        = f.label "Remember me"
    input.w3-button.w3-teal[type="submit" value="Come in"]
    .w3-button.w3-teal
      = link_to "Sing up", new_registration_path(resource_name)
    .w3-button.w3-teal
      = link_to "Forgot your password?", new_password_path(resource_name)